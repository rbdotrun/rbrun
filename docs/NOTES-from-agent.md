# Notes from Agent

`rbrun` is a mountable Rails engine that packages a full agentic Claude SDK runner — sandbox, runtime, persistence, tools, and a component-based conversation UI — as a batteries-included app you can drop into any host Rails app.
