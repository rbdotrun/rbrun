# Example Node Dockerfile for a Kamal preview deploy — adapt to the app.
FROM node:22-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev || npm install --omit=dev

COPY . .
RUN npm run build || true

ENV NODE_ENV=production
EXPOSE 3000
CMD ["npm", "start"]
